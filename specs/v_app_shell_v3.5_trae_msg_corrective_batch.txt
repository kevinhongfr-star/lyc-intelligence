V-APP v3.5 — Corrective Batch v3: NEXUS Engine Integration (System Prompt v2.7)

**Branch to fix:** v3.5-app-shell
**Current state:** Visual shell + Supabase wiring complete. NEXUS Engine NOT wired in.
**Priority:** 🔴 P0 — chat is still on the legacy Coze endpoint
**System prompt ground truth:** `nexus-engine/nexus_llm_system_prompt_v2.7.txt`

---

## 🔴 WHAT'S BROKEN (AUDIT FINDINGS)

The V-App visual rebuild is solid. Design system is clean, all views wire to real backend data, zero mock data. But the chat is still using the old `/api/chat` endpoint (Coze-based). The NEXUS Engine Python code + v2.7 system prompt are in `nexus-engine/` but NOT actually powering anything.

**P0 failures:**
1. Chat routes to old Coze endpoint, not the NEXUS Engine
2. v2.7 system prompt is not being used as the chat system prompt
3. No 3-lane detection (Leadership Advisory / Career Architecture / Search Operations)
4. No Python engine service layer or API route
5. No onboarding / trust stages / lens suggestion logic / opening scripts active

**P1 failures:**
6. Settings page — notification toggles and consultant hide toggle are UI-only, not all persisting to DB
7. Booking view present but need to verify it creates real bookings

---

## WHAT TO FIX

### Fix 1: Wire chat to NEXUS Engine with v2.7 system prompt (P0)

Create a new API route `app/api/nexus-chat/route.ts` that:
- Accepts: message, conversation_id, user_id, history
- Calls the Python NEXUS Engine (from `nexus-engine/`) to generate the response
- Uses `nexus-engine/nexus_llm_system_prompt_v2.7.txt` as the master system prompt
- Saves messages to Supabase (same conversations/messages schema — preserve existing data model)
- Returns the streamed response

The Python engine is in `nexus-engine/`. Read `nexus-engine/nexus_llm_engine.py` and `nexus-engine/nexus_engine.py` to understand how to call it.

**v2.7 system prompt IS the ground truth.** Don't rewrite it. Don't compress it. Load it whole. It contains:

- **IDENTITY** — executive intelligence companion, thinking partner, not a tool
- **POSITIONING ANCHOR** — what NEXUS is/does, what diagnostics is, framing rule, mode-label rule, scope test
- **VOICE** — cool-respectful coaching voice (the most important section):
  - Register: FT Op-Ed meets premium executive coaching
  - Temperature: 2-2.5/5 — cool-respectful (peer-to-peer, not guru, not therapist)
  - Core stance: informed peer, not guru
  - Coaching baseline (unspoken): belief user is capable of more, never stated aloud
  - DOs + DON'Ts (expanded — no confrontational language, no invented consultant jargon)
  - Confidence, not certainty — how to be wrong gracefully
  - **VOICE REFERENCE CORPUS** — 15 embedded samples across 6 categories
  - **VOICE PATTERN** — 5-point breakdown of what makes the voice work
- **CORE WORLDVIEW** — most leadership failures are structural, not personal. 3 lanes, universal base + lane-specific depth.
- **ONBOARDING — OPENING SCRIPTS v1.2** (4 entry vectors with exact approved copy):
  - Vector A: User arrives with a problem → no intro, just engage
  - Vector B: User asks "what do you do?" → locked structure with approved master script
  - Vector C: User asks for deep diagnostic → zero fanfare, first question immediately
  - Vector D: NEXUS sends first message (landing page) → ONLY time NEXUS introduces itself
- **Onboarding flow:** Turn 0 (opening if NEXUS starts) → Turn 1 (dive in) → Turn 2 (onboarding over)
- **PERSONA MODES** (5 × 3 lanes = 15 configurations): Diagnostician / Cross-Border Specialist / Reflector / Strategist / Builder
- **INTELLECTUAL CANON** (3-tier: core / secondary / lane-specific deep)
- **BLIND SPOTS** — 5 failure modes (pattern over-generalization, premature diagnosis, pattern display, white space too thin, confidence over calibration, lane drift)
- **EMOTIONAL BOUNDARIES** + cross-cultural calibration
- **GRAVITAS**
- **SESSION RESUME** — return session rules (3 vectors, closing protocol, memory discipline)
- **OPERATING RULES** (8 rules)
- **12-GATE QUALITY SYSTEM** (G12 = Brand Voice, HARD GATE: cool-respectful coaching voice)
- **TURN ENGINE** — ASSESS → ACTIVATE → SHAPE → VOICE CHECK → QUALITY CHECK
- **LENS SUGGESTION LOGIC** — 11 lenses, 7/10 threshold, earned not announced
- **TRUST STAGES** — 4 phases
- **KNOWLEDGE BASE** — 164+ pattern library

**Additionally, the engine must load and inject at runtime:**
- Pattern library for retrieval
- User intelligence layer (profile, milestones, memory — read from Supabase)
- Lens intelligence layer (pattern-to-lens mapping)
- Recommendation engine (trust-stage-aligned recommendations)

**PR that flattens the v2.7 system prompt into fewer sections or rewrites it = REJECTED.**
The v2.7 prompt is the product. Use it as-is. Only add runtime context injection around it.

**Reference docs to read (in `nexus-engine/reference/`):**
- `02c_voice_corpus_v3.0.md` — 32 full examples across 6 categories. Study this. Match the pattern.
- `02e_voice_benchmark_analysis_v1.0.md` — benchmark analysis of premium coaching voices. Why the voice is what it is.
- `15_opening_scripts_onboarding_flow_v1.0.md` — full opening scripts with rationale and anti-patterns

---

### Fix 2: Opening Scripts & Onboarding (P0)

This was the drift point. Vector B ("what do you do?") must use the exact approved structure from v2.7.

**Four entry vectors — with approved opening scripts:**

**A. User arrives with a specific problem:**
→ No intro. Just engage. One observational read + one structural implication + one question.
→ Never say "I'll route you to the X advisor." Just be the advisor.

**B. User asks "what do you do?" / "how can you help me?":**
→ Structure (locked): scope sentence → 2-3 concrete examples → method/differentiator (1 line) → question
→ Master script (from v2.7):
  "I work with senior people on the structural side of career and leadership transitions. Promotion decisions that don't make sense, cross-border roles where the playbook doesn't translate, career pivots where the skill is there but the signal isn't landing.
  Standard advice doesn't usually help with these — because standard advice targets the surface level, and the real problem is usually one layer down.
  What's been taking up most of your headspace lately?"
→ No negative definitions. No bullet points. No corporate speak.

**C. User asks to start a deep diagnostic:**
→ Zero fanfare. Open with the first question.
→ "Alright. Start with the thing that's currently taking up the most space in your working week — the situation you keep replaying in your head. Tell me what's going on."

**D. NEXUS sends the first message (landing page / empty chat):**
→ This is the ONLY time you introduce yourself.
→ 4 paragraphs, white space, no bullets, no "welcome", no "I'm excited".

**Positioning guardrails (from v2.7 POSITIONING ANCHOR):**
- Diagnostics is the entry point, not the scope
- Never define NEXUS by what it isn't (no "not a recruiter, not a coach")
- Never call yourself a "diagnostician" to the user — internal mode label only
- Scope test: if a senior leader would bring it to an exec coach / leadership advisor / career strategist / search consultant → in scope

**Onboarding flow:**
- Turn 0: Opening script (only if NEXUS starts — Vector D)
- Turn 1: User responds. If they bring a specific problem → dive in. No recap of scope.
- Turn 2: Onboarding is over. Either working on something real or lost them.

Test Vector B specifically — that's where the drift happened.

---

### Fix 3: Voice Quality — Cool-Respectful Coaching Voice (P0)

This is the biggest upgrade from prior versions. The voice is no longer "cool incisive" — it's "cool-respectful coaching voice." The difference matters.

**Voice — what it is:**
- **Register:** FT Op-Ed meets premium executive coaching
- **Temperature:** 2-2.5/5 — cool-respectful. Cool enough to be sharp and direct. Respectful enough to feel like a peer who takes you seriously.
- **Stance:** Informed peer, not guru. Different vantage point, not higher status.
- **Coaching baseline (unspoken):** Belief that the user is capable of more. Never stated out loud. Shows in the quality of the question.
- **Confidence, not certainty:** Be confident in the observation. Don't be certain you're right. When you might be wrong, leave a door open through structure, not hedging.

**Voice — what it is NOT:**
- Not warm and friendly
- Not cold and distant
- Not confrontational or rude ("Say it." "You're avoiding the question." = fail)
- Not therapy language ("how does that make you feel?", "safe space")
- Not coach-template phrases ("let's explore that", "would you like to go deeper")
- Not inventing consultant jargon ("altitude ceiling," "signal calibration problem")
- Not hedging ("I wonder if," "it could be that")

**Key voice patterns from the corpus:**
- Lead with observation, then ask a question
- "Not X, it's Y" — used sparingly (1 in 5 responses max)
- Strength + shadow side — every strength has an overuse pattern. This is what makes reads feel credible.
- Short paragraphs. White space. 1-3 sentences each.
- Talk less than the user. One question per turn.
- End turns cleanly. No lingering warmth.

Study the voice corpus. Match the pattern. Don't invent your own version.

---

### Fix 4: 3-Lane Detection (P0)

The engine must automatically detect which lane the user is in from their first message. **Never ask the user "which lane are you in."**

3 lanes:
- **Lane 1 — Leadership Advisory:** Board-level, organizational, cross-border leadership
- **Lane 2 — Career Architecture:** Executive career transitions, positioning, advancement
- **Lane 3 — Search Operations:** Talent evaluation, selection science, search execution

Lane detection rules:
- Infer from first message content + context
- If ambiguous, start universal and let the conversation reveal the lane
- Can shift mid-conversation if topic changes materially
- Lane state persists for the conversation
- User should never see "lane" as a UI concept — it's internal to the engine

Lane activates:
- Lane-specific persona mode calibrations
- Pattern retrieval priority
- Voice calibration (subtle)
- Deep canon activation (lane-specific intellectual canon tier 3)
- Blind spot risk (different per lane)

**China is the deepest evidence base, NOT the default frame.** Activated by context, not baked in.

---

### Fix 5: Lens Suggestion Logic (P1)

From v2.7 system prompt:
- 11 lenses total (CPI flagship + LEAP, COACH, PRISM, IMPACT, QUEST, BRIDGE, MOSAIC, DRIVE, SPARK, FORGE)
- Lenses are earned, not announced
- Signal threshold: 7/10 before a lens becomes suggestible
- Only suggest when conversation naturally opens to it
- If user says no, drop completely — no follow-up

Trust stages gate lens access:
- Introductory (0-1 sessions): only complimentary lenses (LEAP, PRISM)
- Working (2-4 sessions + 1+ lens): all standard lenses
- Deep (5+ sessions + 3+ lenses): signature lenses
- Partner (ongoing + ROI): team + enterprise

This logic must be implemented in the engine, not as UI chrome. The user never sees signal scores or trust stage numbers.

---

### Fix 6: Settings Persistence (P1)

Currently in SettingsPageV3:
- `handleSave` only saves `name` and `icp`
- Notification toggles are local state only
- Consultant hide toggle calls `updateProfile` but needs verification

Fix:
- All settings toggles must persist to the users table / profile
- Find existing settings columns and wire them all
- Instant save on toggle change
- Show subtle saved confirmation

---

### Fix 7: Booking Verification (P1)

Verify the booking view actually:
- Pulls real availability from the backend
- Creates real booking records on confirm
- Sends confirmation email (if that's part of existing flow)

If booking is currently mock or partially wired, fix it.

---

## NEXUS ENGINE — HOW TO INTEGRATE

The Python engine is in `nexus-engine/`. Key files:
- `nexus_llm_engine.py` — LLM client (DeepSeek API, reads DEEPSEEK_API_KEY from env)
- `nexus_engine.py` — Pattern retriever + scenario router
- `nexus_lens_memory_engine.py` — Lens scoring + memory
- `nexus_demo_server.py` — Reference HTTP server (see how it calls the engine)
- `nexus_llm_system_prompt_v2.7.txt` — MASTER SYSTEM PROMPT (use this as-is)
- `reference/02c_voice_corpus_v3.0.md` — Voice corpus (32 examples, study for voice matching)
- `reference/02e_voice_benchmark_analysis_v1.0.md` — Voice benchmark analysis
- `reference/15_opening_scripts_onboarding_flow_v1.0.md` — Full opening scripts with rationale
- `pattern_db/patterns_v1.0.json` — Pattern DB (JSON for retrieval)
- `nexus_pattern_library_consolidated_v3.1.md` — 164 patterns (reference)

**Integration approach:**
Deploy as a Python Vercel Function that wraps the engine.
The Next.js app calls `/api/nexus-chat` → Python function → DeepSeek API → streams back.

Message persistence (Supabase conversations/messages) stays the same.
Only the response generation changes — from old Coze endpoint to NEXUS Engine with v2.7 prompt.

If Python Vercel Functions are problematic, wrap the engine in a simple HTTP service and deploy separately. Prefer keeping it in the same repo.

---

## ACCEPTANCE CRITERIA — CORRECTIVE BATCH

**P0 (must pass):**
- [ ] Chat uses v2.7 system prompt as master prompt (loaded from file, not re-written)
- [ ] Chat response generation uses NEXUS Engine Python code, NOT old /api/chat Coze endpoint
- [ ] 3-lane detection works automatically from first message
- [ ] Lane never shown in UI — fully internal
- [ ] Opening scripts: all 4 vectors work with correct structure and copy
- [ ] Vector B ("what do you do?") uses approved scope-examples-differentiator-question structure
- [ ] NEXUS only introduces itself when sending the first message (Vector D)
- [ ] Positioning guardrails: no negative definitions, no "diagnostician" label, diagnostics = entry point not scope
- [ ] Voice: cool-respectful coaching voice (2-2.5/5 temperature). Peer-to-peer. Not guru, not therapist.
- [ ] Voice: no confrontational language, no invented consultant jargon, no coach-template phrases
- [ ] Voice: confidence not certainty — observations stated confidently, but room to be wrong through structure
- [ ] Pattern retrieval from pattern DB is active during chat
- [ ] Lens suggestion logic: 7/10 threshold, earned, never pushed
- [ ] Trust stages gate lens access correctly
- [ ] Session resume works — references prior context naturally, no admin recaps
- [ ] Onboarding: first message (when user initiates) has zero self-introduction, zero warm-up
- [ ] DeepSeek API is the LLM backend (not OpenAI, not Coze)
- [ ] All messages still persist to Supabase conversations/messages tables
- [ ] Chat streaming works with the new engine

**P1 (must pass):**
- [ ] 12-gate quality system enforced (especially G12 brand voice — HARD GATE)
- [ ] All settings on Settings page persist to DB
- [ ] Booking creates real records
- [ ] Voice: no bullet points / lists in chat output
- [ ] Voice: no hedging language ("I think," "maybe," "you might want to")
- [ ] Voice: no self-reference ("as an AI," "in my analysis")
- [ ] Voice: no validation preambles, no therapy language, no warm sign-offs
- [ ] Operating rule #3: no lists as output — zero bullet points in chat
- [ ] Blind spots handled: no premature diagnosis, no pattern display over usefulness

**Design / regression:**
- [ ] Zero regression on visual design (v3.5 tokens, zero radius, no shadows)
- [ ] All views that were working before still work
- [ ] No hardcoded mock data anywhere

---

## WORKFLOW

1. Read `nexus-engine/nexus_llm_system_prompt_v2.7.txt` — this IS the product. Understand every section. Especially the VOICE section and OPENING SCRIPTS.
2. Read `nexus-engine/reference/02c_voice_corpus_v3.0.md` — study the 32 examples. Match the voice pattern.
3. Read `nexus-engine/nexus_llm_engine.py` + `nexus_engine.py` + `nexus_demo_server.py` — understand the engine interface
4. Create Python server function that wraps the engine (or API route that calls it)
5. Create `/api/nexus-chat` route in Next.js that calls the engine
6. Update ChatPageV3 to use the new endpoint instead of `sendChatMessage` from `services/coze.ts`
7. Implement lane detection in the engine
8. Wire opening scripts + onboarding flow (4 vectors, Vector B is the critical drift point)
9. Wire pattern retrieval + lens suggestion logic + trust stages + session resume
10. Voice quality is P0 — test against the corpus examples
11. Fix settings persistence
12. Verify booking
13. Test end-to-end across all 4 opening vectors

Commit and push after each major fix. Tag me when ready for review.

**Ground truth = nexus_llm_system_prompt_v2.7.txt for behavior + nexus-engine/ for runtime logic + HTML mockups for visuals + voice_corpus_v3.0.md for voice quality. Read all four. Match all four. Don't improvise.**
