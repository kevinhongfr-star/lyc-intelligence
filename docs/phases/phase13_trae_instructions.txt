PHASE 13 — NEXUS GO-LIVE BRAND SPRINT
PRIORITY: CRITICAL — Go-Live Blocker
Branch: feature/eo4-b2c-portal-phase6
Built on: Phase 12 (CPI v2.0) PASS — commit 8d485ea

========================================================
CORE CONCEPT — READ THIS FIRST
========================================================

NEXUS IS THE PRODUCT. Everything else lives inside it.

- LYC Intelligence = the brand
- NEXUS = the product (AI leadership advisor)
- DEX AI = the engine behind it (attribution only, never a sellable product)
- Assessments, match analysis, market intel = capabilities inside NEXUS

Your job: rebuild the entire public-facing surface to tell this story clearly,
consistently, and on-brand. No more 8 separate "product" pages. One hero product,
clear supporting capabilities, unified design system.

========================================================
DELIVERABLES (10 total)
========================================================

1. NEXUS hero marketing page at /nexus
2. Main landing (/) rebuilt NEXUS-centric
3. DEX AI credibility page at /dex-ai (LYC design system + teal accent)
4. Deprecated routes + 301 redirects (kill old /dex dark navy, /b2c, /assessment, /cpi)
5. Pricing page standardized around NEXUS (3 tiers)
6. All "free" language → brand-compliant terminology
7. Internal terminology leak scan + fix (TICKET-15 P0)
8. Nav naming cleanup (remove all codenames from user-facing labels)
9. CPI URL branding (/china-leadership-pipeline)
10. Zero regressions — Phase 7-12 all still work

========================================================
DESIGN SYSTEM RULES — NON-NEGOTIABLE
========================================================

All public pages use LYC Intelligence design system:
- Typography: Libre Baskerville (headings), DM Sans (body)
- Accent color: #C108AB (magenta)
- Border radius: 0px (sharp corners)
- Max content width: ~900px
- Light background (white / light gray)
- Same header and footer components across all pages

DEX AI PAGE ONLY (/dex-ai):
- Base = same LYC design system (NOT dark navy)
- Single accent: teal/cyan (use #0D9488 as placeholder — adjust later if needed)
- "Powered by DEX AI" appears as a small badge/attribution, NEVER as hero copy
- Dark navy variant is NOT approved — do not use it

"Powered by DEX AI" rule:
- Can appear in small print, badges, footnotes
- Cannot be a headline, hero element, or read as a separate product
- It's the engine inside NEXUS, not something you buy

========================================================
DELIVERABLE 1 — NEXUS MARKETING PAGE (/nexus)
========================================================

File: src/pages/NexusLanding.tsx
Route: /nexus

Build a proper product marketing page for NEXUS.
Use Landing.tsx and B2BLanding.tsx as design system references.

Page sections (top to bottom):

1. HEADER — same component as main landing
   Nav items: NEXUS, For Business (→ /b2b), Pricing (→ /pricing)
   CTA button: "Start — Executive Introduction"

2. HERO
   - Big headline positioning NEXUS as AI leadership advisor
   - Subhead: one sentence on what it does
   - Primary CTA: "Start — Executive Introduction"
   - Secondary: "See how it works" (anchor link down)
   - Small "Powered by DEX AI" badge below CTAs

3. WHAT NEXUS DOES (feature grid — 4 items)
   - AI Advisory Chat (primary)
   - Leadership Assessments (feature China Leadership Pipeline Diagnostic)
   - Candidate Match Analysis
   - Market Intelligence

4. HOW IT WORKS (3-step flow)
   - Start a conversation
   - Get personalized insights & assessments
   - Take action with recommendations

5. FEATURED CAPABILITY — China Leadership Pipeline Diagnostic
   - Spotlight section on the flagship assessment
   - CTA: "Take Complimentary Assessment"

6. FOR BUSINESS TEASER
   - Enterprise / team use cases
   - Link to /b2b

7. PRICING TEASER
   - 3 tiers: Executive Introduction / Executive Access / Enterprise
   - Link to /pricing

8. FINAL CTA SECTION
   - Big CTA: "Start — Executive Introduction"

9. FOOTER — same component as main landing

Requirements:
- Fully responsive (mobile, tablet, desktop)
- Exact same design tokens as Landing.tsx
- Reuse existing components where possible (Header, Footer, etc.)
- All copy = NEXUS as the product

========================================================
DELIVERABLE 2 — MAIN LANDING REBUILD (/)
========================================================

File: src/pages/Landing.tsx
Route: /

Current issue: shows a grid of 3 products like it's a portfolio.
Fix: make it NEXUS-centric. NEXUS is the hero.

Structure:
1. Header (update nav labels — see Deliverable 8)
2. Hero — NEXUS headline + primary CTA
3. "What NEXUS Does" section — 4 capability cards (chat, assessments, match, market intel)
4. Social proof / trust section (keep whatever makes sense from current)
5. CTA section
6. Footer (update links — see Deliverable 8)

Keep the existing design system, just reframe the content.
Don't change components that aren't related to the message.

========================================================
DELIVERABLE 3 — DEX AI CREDIBILITY PAGE (/dex-ai)
========================================================

File: src/pages/DexAiPage.tsx
Route: /dex-ai
Replaces: old /dex route (dark navy version)

This is NOT a product page. It's a technology credibility page.
People come here to understand what powers NEXUS.

Design: LYC Intelligence base system + teal accent (#0D9488)
NO dark navy. NO standalone visual identity.

Sections:
1. Header — same as other landing pages
2. Hero — "Powered by DEX AI" headline. Explain it's the intelligence engine.
   Subhead: what it enables inside NEXUS
3. Architecture overview — simple visual/text explanation of how DEX AI works
   (multi-agent system, assessment engines, matching algorithms, etc.)
4. Trust & Security section — data handling, privacy, reliability signals
5. Back to NEXUS CTA → /nexus
6. Footer

Keep it concise. 3-4 sections max. This shouldn't feel like a sales page.

========================================================
DELIVERABLE 4 — DEPRECATED ROUTES + REDIRECTS
========================================================

Create 301 redirects for:
/dex → /dex-ai
/b2c → /nexus
/assessment → /china-leadership-pipeline
/cpi → /china-leadership-pipeline

Implementation:
- Use Next.js redirects (next.config.js or in-component redirects with useEffect)
- Make sure redirects are 301 (permanent), not 302

Cleanup:
- Remove old dark navy DexLandingPage.tsx from the /dex route
- Keep the Dex*Page components (DexJourneyPage, DexPlanPage, etc.) in the codebase
  but remove them from public routing. We may repurpose them later.
- Update ALL internal links across the codebase to point to new URLs
  - Search for href="/dex", href="/b2c", href="/assessment", href="/cpi
  - Update nav components, landing pages, footer, CTA buttons

========================================================
DELIVERABLE 5 — PRICING PAGE STANDARDIZATION
========================================================

File: src/pages/PricingPage.tsx
Route: /pricing

Standardize around NEXUS as the product.

3 tiers:
1. Executive Introduction (free tier — use the name, never "free")
2. Executive Access (paid individual)
3. Enterprise (B2B)

Required fixes:
- All "Free" → "Executive Introduction" (use the tier name)
- Tier names match exactly: Executive Introduction / Executive Access / Enterprise
- Feature lists reframed as NEXUS features
- CTAs use brand-compliant language
- Keep whatever content/paywall integration already exists

========================================================
DELIVERABLE 6 — "FREE" LANGUAGE REMOVAL
========================================================

Scope: ALL user-facing copy across all pages and components

Rules:
- "Free Assessment" → "Complimentary Assessment"
- "Try Free" → "Start — Executive Introduction"
- "free tier" → "Executive Introduction"
- "Get free X" → "Get complimentary X" or use tier name

Action:
1. Search entire src/ directory for: free, Free, FREE
2. For each match, determine if it's user-facing copy
3. If yes → replace with brand-compliant terminology
4. If no (variable names, comments, code logic) → leave it
5. Verify completion with case-insensitive grep on user-facing files

Note: Be thorough. Check buttons, headlines, body copy, form placeholders,
tooltip text, error messages, success messages — everything the user sees.

========================================================
DELIVERABLE 7 — INTERNAL TERMINOLOGY LEAK SCAN (P0)
========================================================

Scope: ALL user-facing UI (public pages + portal + authenticated surfaces)

Internal terms that must NOT appear user-facing:

INTERNAL TERM → USER-FACING REPLACEMENT
CPI → China Leadership Pipeline Diagnostic
TRIDENT → Match Analysis
GRID → Market Intelligence
CANVAS → Scorecard Builder
flywheel → remove / rephrase (internal architecture term)
Layer 1 / Layer 2 / Layer 3 → remove / rephrase (internal architecture)
archetype (in user context) → leadership profile / leadership type
funnel (in product context) → pathway / journey / progression

Action:
1. Full codebase scan for these terms
2. Check nav labels, page titles, headings, body copy, buttons, forms
3. Replace each with user-facing equivalent
4. Variable names, API routes, database fields, code comments = OK to leave
5. Internal-only admin surfaces = OK to keep (but prefer consistency)

========================================================
DELIVERABLE 8 — NAVIGATION NAMING CLEANUP
========================================================

PUBLIC HEADER NAV:
Update to: NEXUS (→ /nexus), For Business (→ /b2b), Pricing (→ /pricing)
Logo → /

B2C COACHING PORTAL SIDEBAR (/coaching):
Coach → NEXUS Chat
CPI Assessment → Pipeline Diagnostic
Credits → Credits (OK)
Intelligence → Intelligence (OK — not a codename)
Career Intel → Career Intel (OK)
Profile → Profile (OK)

INTERNAL OPS NAV (/app/*):
TRIDENT → Match Analysis
CANVAS → Scorecard Builder
GRID → Market Intelligence
SHIFT → SHIFT Suite (already a product name, OK)

Important: Only change the DISPLAY LABELS.
Do NOT change the actual routes or URLs for internal pages.
/app/trident stays as /app/trident — just the label in the nav changes.

Also update:
- Footer links
- Any breadcrumbs
- Page titles / meta tags

========================================================
DELIVERABLE 9 — CPI URL BRANDING
========================================================

- Create /china-leadership-pipeline route for the public assessment entry
- This should render the assessment landing/wizard (currently at /assessment)
- /assessment → 301 redirect to /china-leadership-pipeline
- /cpi → 301 redirect to /china-leadership-pipeline
- All user-facing copy: "CPI" → "China Leadership Pipeline Diagnostic"
- B2C coaching portal route /coaching/cpi can stay as internal route,
  but its nav label changes to "Pipeline Diagnostic" (see Deliverable 8)

========================================================
DELIVERABLE 10 — ZERO REGRESSIONS
========================================================

Before you call this done, verify:

1. All Phase 7-12 modules still registered in dispatch.ts
2. All existing test files still run and pass
3. No existing pages are broken by URL changes
4. Auth still works on all authenticated routes
5. API endpoints all still reachable
6. No broken links across the site

Run the existing test suite. All tests should still pass.
If any tests break due to legitimate renaming, update the tests.

========================================================
BRAND TERMINOLOGY CHEAT SHEET
========================================================

DO SAY:
- NEXUS (the product)
- LYC Intelligence (the brand)
- "Powered by DEX AI" (engine attribution)
- Executive Introduction (free tier name)
- Complimentary Assessment
- China Leadership Pipeline Diagnostic
- Match Analysis
- Market Intelligence
- Scorecard Builder
- SHIFT Suite

DON'T SAY:
- "DEX AI" as a product to buy
- "free" (use the tier name or "complimentary")
- CPI (user-facing)
- TRIDENT (user-facing)
- GRID (user-facing)
- CANVAS (user-facing)
- flywheel, Layer 1/2/3, funnel (user-facing)
- dark navy DEX AI design system

========================================================
DONE CRITERIA
========================================================

When finished, confirm:
1. /nexus = NEXUS marketing page (not chat)
2. /nexus/chat = chat interface
3. /dex-ai = DEX AI credibility page (LYC design system, NO dark navy)
4. Main landing / is NEXUS-centric
5. Zero "free" in user-facing copy
6. Zero internal codenames in user-facing UI labels
7. "CPI" not in any user-facing copy or URL
8. All 4 deprecated routes 301-redirect correctly
9. Pricing page = 3 NEXUS tiers with correct names
10. All Phase 7-12 functionality still works
11. All existing tests pass
12. Design system consistent across all public pages
