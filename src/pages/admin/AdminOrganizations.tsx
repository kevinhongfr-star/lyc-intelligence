/**
 * AdminOrganizations — Organization management page wrapping OrgSettings.
 */
import React from 'react';
import OrgSettings from '@/components/admin/OrgSettings';

const AdminOrganizations: React.FC = () => {
  return <OrgSettings />;
};

export default AdminOrganizations;
